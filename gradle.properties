org.gradle.jvmargs=-Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Use bundled qemu-wrapped aapt2 for Termux ARM64 builds - set via build-on-termux.sh script
# Commented out to allow CI builds to use standard aapt2 while Termux builds pass it explicitly
# android.aapt2FromMavenOverride=/data/data/com.termux/files/home/git/swype/Unexpected-Keyboard/tools/aapt2-arm64/aapt2

# Workaround for R8/D8 NullPointerException crash with nullable Kotlin types
android.enableR8.fullMode=false
